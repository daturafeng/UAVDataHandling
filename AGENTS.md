# AGENTS.md

## 1. 项目目标

本项目用于处理无人机拍摄图像，当前首要目标是：

- 输入图像中的像素坐标 `(u, v)`。
- 结合图像内元数据或外部传入的无人机状态参数。
- 输出目标点在 `WGS84` 坐标系下的经纬度坐标。

后续功能应围绕“可验证的坐标解算链路”逐步扩展，不应在首阶段同时引入过多无关能力。

## 2. 中文约束

- 本项目中的提问、回答、代码注释、文档说明、界面文案均必须使用中文。
- 非必要情况下不得将项目界面、提示语、注释或文档改成英文。

## 3. 开发原则

- 优先保证坐标计算链路正确，再追求功能丰富。
- 任何坐标解算都必须显式写清坐标系、单位、旋转顺序与符号约定。
- 首阶段默认采用“地面为单一平面”的简化模型；若要引入 `DEM`、地形起伏或高精度摄影测量，必须单独设计。
- 所有关键公式都应能被单元测试或样例数据验证。
- 输入缺失、元数据异常、姿态角不完整时，不允许静默给出结果，必须返回明确错误。

## 4. 已确认外部测试数据

当前项目已确认可直接使用以下真实数据作为首阶段测试样例来源：

- `G:\DJIImage\drone_images`

当前已确认的文件构成：

- `85` 张 `DJI_*.jpeg`
- `2` 个 `*.jpeg.aux.xml`
- `1` 个 `*.MRK`
- `1` 个 `*.NAV`
- `1` 个 `*.OBS`
- `1` 个 `*.RTK`

当前对这些文件的使用约定如下：

- `JPEG` 是首阶段主输入。
- `JPEG` 内嵌的 `XMP` 与 `EXIF` 是首选元数据来源。
- `*.jpeg.aux.xml` 可作为外部展开版元数据的辅助来源，用于调试、核对和补充字段解析。
- `*.MRK` 视为高优先级曝光事件记录，可用于按曝光序号核对 `RTK` 定位结果。
- `*.NAV` 与 `*.OBS` 视为 `GNSS / RINEX` 参考数据，首阶段不要求完整解算，但应保留后续接入能力。
- `*.RTK` 当前视为厂商伴随文件；若其格式不稳定或为二进制，首阶段不强依赖直接解析。

## 5. 建议目录约定

后续新增代码时，优先按如下结构组织：

- `src/uav_geo/`
- `src/uav_geo/models/`
- `src/uav_geo/readers/`
- `src/uav_geo/geometry/`
- `src/uav_geo/services/`
- `tests/`
- `samples/`
- `docs/`
- `web/`

目录职责建议：

- `models`：数据模型，如像素点、相机参数、无人机状态、解算结果。
- `readers`：图像 `EXIF / XMP / 厂商元数据` 读取。
- `geometry`：坐标系转换、姿态旋转、射线与地面求交。
- `services`：对外的核心业务编排接口。
- `tests`：单元测试、样例驱动测试。
- `samples`：脱敏后的样例图片、样例元数据或模拟输入。
- `docs`：算法说明、坐标约定、误差说明。
- `web`：本地网页端界面与前端资源。

## 6. 坐标与单位约束

- 地理坐标默认使用 `WGS84`。
- 经纬度统一使用十进制度数表示。
- 高度必须明确区分：
  - 椭球高
  - 大地高
  - 相对起飞点高度
  - 相对地面高度
- 距离统一使用米。
- 姿态角统一明确：
  - `yaw / heading`
  - `pitch`
  - `roll`
- 必须在文档或代码注释中说明姿态角的正方向定义和旋转顺序。
- 像素坐标原点默认约定为图像左上角，`u` 向右增大，`v` 向下增大。

## 7. 实现约束

- 优先使用纯 `Python` 与成熟数值库实现核心算法。
- 首选依赖：
  - `numpy`
  - `pydantic` 或 `dataclasses`
  - `pytest`
  - `Pillow`
  - `piexif`、`exifread` 或适合的元数据读取库
- 若引入 `pyproj`，必须明确其用途，例如大地坐标与投影坐标转换。
- 不要把“读取元数据”“相机建模”“几何解算”“结果输出”写在一个脚本函数里。
- 不允许把厂商私有字段名硬编码散落在多个文件，必须集中封装。

当前 `DJI` 数据已确认至少包含以下高价值字段，后续读取器实现时应优先覆盖：

- `drone-dji:GpsLatitude`
- `drone-dji:GpsLongitude`
- `drone-dji:AbsoluteAltitude`
- `drone-dji:RelativeAltitude`
- `drone-dji:GimbalRollDegree`
- `drone-dji:GimbalYawDegree`
- `drone-dji:GimbalPitchDegree`
- `drone-dji:FlightRollDegree`
- `drone-dji:FlightYawDegree`
- `drone-dji:FlightPitchDegree`
- `drone-dji:LRFTargetDistance`
- `drone-dji:LRFTargetLon`
- `drone-dji:LRFTargetLat`
- `drone-dji:LRFTargetAlt`
- `drone-dji:RtkStdLon`
- `drone-dji:RtkStdLat`
- `drone-dji:RtkStdHgt`
- `drone-dji:UTCAtExposure`

## 8. 数据优先级约束

当同一语义字段来自多个来源时，首阶段默认优先级为：

1. 外部显式传入参数
2. `JPEG` 内嵌 `XMP` 中的 `drone-dji:*` 字段
3. `*.jpeg.aux.xml` 中展开后的 `DJI / EXIF` 字段
4. 基础 `EXIF GPS` 字段
5. `*.MRK` 中可按曝光记录关联到的外部定位字段

说明：

- 若多个来源值冲突，不允许静默覆盖，必须记录来源并保留诊断信息。
- 若 `MRK` 与 `JPEG` 内嵌 `XMP` 可建立可靠曝光序号映射，后续可提升 `MRK` 在 `RTK` 场景中的优先级。

## 9. 前端界面约束

- 网页界面应尽量简洁，只保留用户当前真正需要的功能。
- 非必要说明文字、调试信息、标签展示、冗余面板不应默认显示在主界面上。
- 当前项目网页端优先保留：
  - 左侧地图
  - 右侧图片选择
  - 图片显示区域
  - 点、线、面绘制工具
- 调试信息、元数据摘要、内部状态提示如非必要，应隐藏或移出主界面。

## 10. 测试要求

- 每个核心几何函数必须有单元测试。
- 至少准备一组可重复验证的标准样例：
  - 已知无人机位置
  - 已知姿态
  - 已知相机内参
  - 已知地面点真实经纬度
- 对以下场景必须覆盖测试：
  - 图像中心点解算
  - 图像四角点解算
  - 无元数据、只传入外部状态
  - 姿态角或高度缺失
  - 相机朝天或接近地平线导致无法与地面相交

首阶段建议直接从 `G:\DJIImage\drone_images` 中挑选少量代表性样本建立回归测试基线。

## 11. 文档要求

- 每新增一个核心模块，都应说明输入、输出、坐标系和单位。
- 在 `docs/` 中维护一份坐标解算说明，至少包含：
  - 坐标系定义
  - 相机模型
  - 旋转链路
  - 地面交点求解方法
  - 误差来源
- 若针对特定无人机品牌或型号做适配，必须记录其元数据字段来源与适用范围。

## 12. 变更纪律

- 小步提交，先让结果可跑、可测、可解释，再继续扩展。
- 修改算法时，必须同步更新测试与文档。
- 若发现同一字段在不同机型中语义不一致，优先补充兼容层，不直接修改公共语义。
- 未验证的假设必须在代码注释或文档中明确标记，不得伪装为确定事实。

## 13. 首阶段范围边界

当前阶段优先完成：

- 单张图片读取元数据。
- 支持外部传入无人机状态覆盖图像元数据。
- 输入单个像素坐标，输出对应 `WGS84` 经纬度。
- 基于平地假设完成首版几何解算。
- 支持单图多点、线、面的批量地理求解。
- 提供最小可验证 `CLI`、`Python API` 与本地网页端。

当前阶段暂不默认纳入：

- 正射校正
- 批量拼接
- 多视图重建
- `DEM / DSM` 地形修正
- 亚米级高精度误差建模
